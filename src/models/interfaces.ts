export interface EventRow {
    id: number;
    image: string;
    title: string;
    description: string;
    imagePosition: 'left' | 'right';
}

export interface EventCard {
    id: number;
    image: string;
    title: string;
    description: string;
}
